# Subsystem inventory (W2 — complexity budget)

One row per long-lived subsystem. Add or update the row when one is introduced or materially
changed, and explain what it replaces or why net surface grows. Include a justifying eval and
committed floor when the subsystem has meaningful measurable behavior; ordinary plumbing does
not need an artificial metric. CI checks only structural path coverage via
`scripts/check-subsystems.ts`.

Fields: **What/where** (paths are checked by CI) · **Justifying eval + delta** · **Deletion
cost** · **Dependencies** · **Maintenance** (DECISIONS.md entries touching it).

| Subsystem | What / where | Justifying eval + delta | Deletion cost | Dependencies | Maintenance |
|---|---|---|---|---|---|
| MCP door | `src/mcp/`, `src/serve.ts`, `src/util/postgres-url.ts`, `src/util/audit-payload.ts` (20 tools, envelope/redaction/exact verb-bound audit payloads, complete returned-row source IDs, and an app-only stdio child with an environment allowlist behind a non-MCP maintenance supervisor) | m2 tools suite, including edge/task/commitment transport completion IDs; m6 leak suite (200 fuzzed calls, exact attempt/result/disposition accounting, zero tier leaks); privacy-hardening locked-source audit cases; audit-payload constructor/verb/sentinel suite; H4 raw-wire/refusal, disposition, send-outcome, and facade-owner race suites; serve-boundary ambient-token and audit-sentinel tests | the product stops existing (I2) | @modelcontextprotocol/sdk | DECISIONS.md § 2026-06-11 — Skills layer expansion + minime_review_queue tool, § 2026-06-15 — Redaction carves out canonical UUIDs, § 2026-06-18 — MCP calls accept caller timezone for user-local time semantics, § 2026-07-23 — H4: raw-receipt attempt and pre-release result auditing, § 2026-07-26 — H4 result authorization, local disposition, and identity-owned facade, § 2026-08-06 — Local runtime and engineering surfaces are closed by default; net surface +1 (W1-2: minime_list_metrics — no-SQL metric catalog for agent discovery, closing the UNKNOWN_METRIC dead end); net surface +1 (W2-3: minime_refile — files a pending inbox capture as a typed row under the anti-laundering evidence floor, closing the triage dead end); net surface +1 (W2-4: minime_correct — the first content-mutation tool beyond tasks: amend/retract/retier for typed rows through one audited call, bounded by the same tier-read floor as every other tool); net surface +1 (W2-6: minime_upsert_person — alias/relation/rename so misspellings stop minting permanent duplicates, alias tier inherited from the entity); net surface +1 (W3-3: minime_timeline — tier-gated date-range read across calendar/tasks/decisions/journal/interactions via one UNION ALL query, closing the "summarize my June" dead end; locked tier-2 rows disclose only a bare per-kind count); interface extended, no new tool (W3-7: minime_state gains an ops_health block — dream_last_at/failed_steps/ops_failure_open, content-free and tier-independent; minime_review_queue gains the ops_failure kind, also content-free); net surface +1 (W3-10: minime_set_person_date — birthdays/anniversaries/custom recurring person dates, insert/update only, no delete; minime_state gains the upcoming_dates block it feeds, folded into the morning brief's existing "Coming up" section rather than a second near-duplicate heading); net surface +1 (W3-12: minime_upsert_goal — create/update a goal across all three horizons life/year/quarter, closing both the missing-update-path and the unreachable-quarter-horizon dead ends; minime_state gains goals_active — open-task counts and last linked-task activity per active goal, tier-bounded; minime_review_queue gains the goal_review kind — a 90d-stale goal with no linked-task activity, resolved to its statement fresh at the caller's tier like decision_review); interface extended, no new tool (W4-2: minime_review_queue gains the entity_promotion kind — a person/org identity still stuck at tier 2, resolved to its name fresh at the caller's tier like phantom_person/goal_review; the actual 2→1 demotion is owner-CLI-only, `entity:restore-tier` in `src/cli.ts`, never reachable through MCP — this row's tool surface can flag and read, never execute the tier change); interface extended, no new tool (W4-4: minime_search's `gaps` now includes a bare tier-2 locked-match count when a locked query matches tier-2 content — schema/params unchanged, only the description and `gaps` content grew); net surface +1 (W4-6: minime_log_expense — the first agent write path into tier 0, insert-only with no read-back, self-dedupe on a deterministic id, and a review-queue flag for a colliding future bank-CSV import; `db/migrations/040_transactions_note.sql` adds a nullable `note` column and no grant) |
| Tier/RLS enforcement | `src/db/repo.ts`, `src/mcp/server.ts`, `src/mcp/tools/unlock.ts`, `src/cli.ts`, `src/util/metric-templates.ts`; `db/migrations/007_rls.sql`, `db/migrations/013_privacy_hardening.sql`, `db/migrations/022_entity_derivation_tiers.sql`, `db/migrations/023_session_unlock_approval.sql`, `db/migrations/024_engineer_content_boundary.sql`, `db/migrations/026_time_semantics.sql`, `db/migrations/029_scope_entity_conflicts_by_tier.sql`, `db/migrations/032_timeline_locked_count.sql`, `db/migrations/037_identity_content_tier_split.sql`, `db/migrations/038_entity_promotion_backfill.sql`, `db/migrations/039_suppressed_hit_count.sql` | m6 leak suite; metric-time semantics and runtime-role aggregate-door/cache-write cases; privacy-hardening suite; entity-tier-provenance suite; entity-tier-split suite (guarded-trigger demotion/tier-0-transition adversarial cases); entity-tier-restore suite (backfill, ongoing-detection hook, restore CLI path, MCP masking, defensive refusals); tier-split-leak suite (W4-3: one mixed-tier fixture — an owner-known identity later mentioned privately, a person who exists only through a private mention, a restored legacy tier-2 identity — driven through every MCP read door in a single never-unlocked session, then re-driven unlocked; pins the last_contact_at timing disclosure to an exact before/after value and proves no MCP tool path carries a tier-demotion lever; extends m6 leak suite's own fuzz matrix with a tier-2-only identity and minime_upsert_person/minime_review_queue calls); unlock-approval suite; m15 explicit engineer allowlist/future-default-deny cases; person-tool suite's tier-oracle regression tests (minime_upsert_person add_alias/rename vs. a hidden tier-2 conflict); suppressed-hits suite (W4-4: minime_search's tier-2 locked match count — locked/unlocked/zero-match/oracle-opacity cases, type-scoped exclusion/inclusion cases (review finding, 2026-08-09: the count now mirrors the caller's own `types` filter) plus a real restricted-role RLS-sanity check that a raw candidate query without the tier ceiling still cannot surface tier-2 rows); tier0-cli-read suite (W4-5: `tx list`/`health list` month/kind/range filtering and LIKE-escape correctness, count-only audit payload shape, non-TTY refusal vs. the `MINIME_ALLOW_NON_TTY_TIER0` test seam, source-level pin that only the one TTY-gated render function ever calls console.log on a row); metric-template suite (W4-8: all five templates generate the fixed period_start/value/label shape and execute correctly via metric_agg on fixture data; injection-attempt rejection before any SQL is generated; duplicate-name/invalid-name rejection; a broken-template dry-run rollback via a test double so a bad def can never persist; 027's seeded body_mass/hr_resting defs answering minime_query_metric over fixture data, closing the UNKNOWN_METRIC repro without a new migration; a metric:add CLI-subprocess case proving the audited `{metric, template}`-only payload); unlock-lifecycle suite (W4-12: pendingAndActiveUnlocks pending/active/excludes-expired; revoke fails closed mid-transaction on the restricted app role — the mandatory guard proving 023's app_allowed_tier() re-checks expires_at per statement, not per session; fixed unlock:tier2:revoked audit payload alongside approved/requested; resident supervisor's per-request line proven id/minutes-only, never the requesting actor or session id; unlock:status/unlock:revoke CLI paths run ahead of the Ollama preflight) | agents read everything — never | Postgres RLS; local owner CLI | DECISIONS.md § 2026-06-10 — agg_sql convention: ($1,$2) params, 3-column result, security definer door; migration 013 has no dedicated DECISIONS.md entry — gap noted, not backfilled here; DECISIONS.md § 2026-08-06 — Tier-2 reads require session-bound owner approval, § 2026-08-06 — Derived identities inherit source privacy and provenance, § 2026-08-06 — Local runtime and engineering surfaces are closed by default, § 2026-08-06 — Calendar days and metric buckets use explicit time zones, § 2026-08-08 — Entity-conflict checks are bound to the caller's own tier, § 2026-08-09 — Identity/content tier split: resolving an existing identity no longer promotes its tier (W4-1, migration 037's guarded 2→1 demotion mechanism), § 2026-08-09 — Entity demotion path: owner-CLI-only restore, entity_promotion review kind, migration 038 backfill (W4-2), § 2026-08-09 — W4-4: minime_search discloses a tier-2 locked match count (migration 039's suppressed_candidate_count()), § 2026-08-10 — W4-5: owner-terminal tier-0 CLI reads (`tx list`/`health list`), a recorded exception to "never log, print, or snapshot tier-0 contents"; net surface +0 MCP tools — owner-terminal-only, never reachable through MCP, § 2026-08-10 — W4-8: `metric:add` CLI — vetted templates are the only non-migration path onto the agg_sql aggregate door; net surface +0 MCP tools — owner-terminal-only, never reachable through MCP; W4-12 adds `unlock:status`/`unlock:revoke` (owner-terminal CLI only, never MCP) plus resident `serve` pending-unlock surfacing (~5s poll, lock-winner-gated same as dream/backup) — revoke closes an active approval by setting `expires_at` to now, which 023's existing `app_allowed_tier()` already re-checks on every read, so no invariant or the approval contract itself changed and no dedicated DECISIONS.md entry was added; net surface +0 MCP tools |
| Correction loop | `src/mcp/tools/correct.ts` (amend/retract/retier), `src/mcp/tools/refile.ts` (manual filing of a pending inbox capture), `src/mcp/tools/person.ts` (alias/relation/rename), `db/migrations/028_correction_supersede.sql` (the shared superseded_by/superseded_at contract + column-limited grants); owner-run `scripts/repairs/merge-person.ts` and `scripts/repairs/retype-org-to-person.ts` for the identity-level fixes no chat tool reaches, and `scripts/repairs/recategorize-transactions.ts` (W4-7: re-applies `config/tx-categories.json`'s current rules to existing tier-0 rows, category-null by default or every row with `--force-all`, counts-only summary) | correct-tool suite (13 cases: amend/retract/retier mechanics, tier-boundary 404s, already-superseded guard); refile suite (17 cases: all five destination types, anti-laundering evidence floor, claim/duplicate races); person-tool suite (12 cases: alias/relation/rename, tier-scoped conflict refusal); merge-person suite (29 cases: alias/interaction/edge repoint, phantom_person auto-resolve, tier-0 quarantine boundary, self-merge refusal); superseded-search suite (5 cases: amend down-weights and labels a hit, retract drops it from search entirely) | no owner-facing fix for a wrong, misfiled, or duplicate row short of a raw SQL edit — every "that's wrong" or "that's the same person" conversation dead-ends | MCP door (hosts the three tools) and Tier/RLS enforcement (028's tier_update floor) rows above; no new external dependency | DECISIONS.md § 2026-08-07 — Content tables can record their own supersession, with a column-limited grant (migration 028, the schema this loop shares with minime_correct), § 2026-08-08 — Unfiled-capture text/reason require a tier-2 unlock; the classifier guess does not (refile's anti-laundering gate), § 2026-08-08 — Entity-conflict checks are bound to the caller's own tier (upsert_person's alias-conflict scoping), § 2026-08-08 — minime_refile interface and its anti-laundering evidence-floor policy; minime_correct/minime_upsert_person/merge-person's own tool-level decisions have no dedicated entries beyond the above — gap noted, not backfilled here |
| Hybrid search core | `src/search/hybrid.ts` (RRF+blend+multipliers) | MinimeBench all areas; retrieval-en hit@1 92.0% (fusion core alone, pre-rerank/pre-OpenRouter — docs/benchmarks/2026-06-12-live-final-minimebench.md). That scorecard is itself superseded as the *binding* live record by docs/benchmarks/2026-06-12-live-qwen3-minimebench.md (94.0%) once the reranker and OpenRouter embeddings layer on — those deltas are credited to their own rows below | no search product | pgvector, FTS | DECISIONS.md § 2026-06-11 — ftsCandidates: OR-rewritten websearch_to_tsquery instead of plainto_tsquery, § 2026-06-12 — Search uplift Phase 1a: RRF fusion replaces the §9 weighted sum, § 2026-06-12 — Fusion calibration cycle: blend change tried, refuted live, reverted, § 2026-06-12 — Provider priority: Bedrock (IAM) LLM layer + OpenRouter embeddings |
| Intent nudges | `src/search/intent.ts` | MinimeBench time/entity areas (zero-LLM) | revert to flat weights; small area losses | none | DECISIONS.md § 2026-06-12 — Search uplift Phase 1a: RRF fusion replaces the §9 weighted sum (introduces the zero-LLM intent classifier) |
| Title-phrase boost | `src/search/title-match.ts` | MinimeBench identity area | identity recall drops | none | DECISIONS.md § 2026-06-12 — Search uplift Phase 1a: RRF fusion replaces the §9 weighted sum (introduces the title-phrase boost) |
| Reranker | `src/search/rerank.ts` (bge-reranker-v2-m3 via llama-server) | LongMemEval recall@1 74.8%→88.6% (committed scorecard; DECISIONS prose says 88.8%) (pre-rerank figure: DECISIONS.md § 2026-06-12 — Phase 3; post-rerank committed scorecard: docs/benchmarks/2026-06-12-longmemeval-s.md); PMB precision 6.5%→52.3% (docs/benchmarks/2026-06-12-live-baseline-precisionmembench.md → docs/benchmarks/2026-06-12-live-rerank-precisionmembench.md) | fail-open exists; revert to RRF order; floors drop to no-rerank baselines | llama.cpp daemon, GGUF file | DECISIONS.md § 2026-06-12 — Phase 3: local cross-encoder reranker + autocut (§4 amendment), § 2026-06-12 — Provider priority: Bedrock (IAM) LLM layer + OpenRouter embeddings |
| Autocut | `src/search/autocut.ts` | PMB precision experiment (fails open on flat curves — documented negative result) | keep fixed top-k | reranker scores | DECISIONS.md § 2026-06-12 — Phase 3: local cross-encoder reranker + autocut (§4 amendment), § 2026-06-12 — PrecisionMemBench runner (89 cases, judge-free retrieval precision) — same-day addendum "autocut calibration cycle, concluded negative" |
| Chunker + CJK | `src/search/chunker.ts`, `src/util/cjk.ts`, migrations 009/010 | m8 CJK suite; retrieval-zh area (hit@3 ≥ 95%) | zh retrieval collapses | none | DECISIONS.md § 2026-06-11 — M8: CJK-aware FTS (bigram fold) and chunk sizing, § 2026-06-12 — CJK FTS lexemes go ASCII-hex (010): macOS libc broke Han tokenization |
| Provider layer + egress audit | `src/llm/`, durable audit path in `src/db/client.ts` and `src/db/repo.ts`, `src/util/ollama-url.ts`, `src/llm/ollama-http.ts`, `scripts/lib.sh` | providers suite (request shapes, tier gate, intent/outcome persistence across handler rollback, and five-connection starvation regression); shared corpus/direct-network tests (URL parity, preflight ordering, proxy/redirect refusal, deadlines) | local-only Ollama fallback | per-provider creds; direct loopback transport (`src/llm/ollama-http.ts`) and shell bootstrap (`scripts/lib.sh`) | DECISIONS.md § 2026-06-11 — Cloud LLM providers (amends invariant I1) + two new SDK dependencies, § 2026-06-11 — Correction: OpenRouter DOES serve embeddings; enabled at 768 dims, § 2026-06-12 — Provider priority: Bedrock (IAM) LLM layer + OpenRouter embeddings, § 2026-07-23 — H2: loopback-only Ollama, § 2026-08-06 — Local runtime and engineering surfaces are closed by default |
| Embedding pipeline | `src/search/embed.ts`, `index-parent.ts` | m3 retrieval eval; MinimeBench | FTS-only search | Ollama/OpenRouter | DECISIONS.md § 2026-06-11 — Correction: OpenRouter DOES serve embeddings; enabled at 768 dims, § 2026-06-12 — Provider priority: Bedrock (IAM) LLM layer + OpenRouter embeddings |
| Edge extractor (zero-LLM) | `src/pipeline/extract-edges.ts` | m7 graph suite; MinimeBench graph area (15/15) | graph boost + works_at answers gone | none | DECISIONS.md § 2026-06-11 — M7 (post-v1 feature): typed knowledge-graph extraction, zero-LLM, § 2026-06-16 — Extractor: ingestion-time guard against phantom "org" nodes, § 2026-06-16 — Family-relation people never get works_at edges |
| Extractor re-validation | `src/pipeline/validate-edges.ts` | m14 planted-corpus CI bar (3/3 flagged, 0 false) green via verify-m14; live scorecard pending (make eval-graph-hygiene, owner-run) | phantom-entity class reopens | classify provider | DECISIONS.md § 2026-07-18 — W1: nightly extractor re-validation |
| Compiled notes | `src/pipeline/notes.ts`, `src/pipeline/brain-sync.ts`, `src/pipeline/compiled-note-recovery.ts`, `src/util/compiled-note-archive.ts`, tier/evidence guards in `src/db/repo.ts` and `src/search/index-parent.ts`, migration `019_tier0_prose_quarantine.sql` | H1 regression suite (codec, sync, recovery, collision, interleaving, sentinel, absorbing tier-0 quarantine, RLS lower bound, and zero-egress blocking) as maintenance evidence; ×1.5 boost **eval-calibration pending** — no committed Minime-side lift scorecard yet; "largest documented lift" is GBrain's (+8 pts on their benches, DECISIONS.md § 2026-06-12) | biggest retrieval regression of any single deletion; removing quarantine reopens tier-0 prose exposure | dream job, PostgreSQL RLS | DECISIONS.md § 2026-06-12 — Search uplift Phase 2: compiled-notes layer (§15 early adoption) + NOTES_BOOST (I5 amendment); DECISIONS.md § 2026-07-23 — H1: canonical compiled archives and reconciliation recovery; DECISIONS.md § 2026-07-25 — H1 tier-0 absorbing quarantine and policy correction |
| Decision engine + interview + digests | `src/mcp/tools/decisions.ts`, `src/pipeline/decision-digest.ts`, `src/util/clock.ts`, migration 014 | m5 suite, including DST calendar-day review dates; metric-time owner-local due-enqueue case; decision-digest area (hit@3 = 1.0 provisional) | decisions become flat rows | none | DECISIONS.md § 2026-06-17 — Decision interview source layer + branch graph + retrieval digest, § 2026-06-17 — Same UTC-slice date bug in two write tools (journal title, decision review_at), § 2026-08-06 — Calendar days and metric buckets use explicit time zones |
| Watcher + classifier | `src/pipeline/watcher.ts`, `classify.ts`, `dedup.ts`, `src/util/atomic-file.ts`, `db/migrations/025_inbox_capture_identity.sql` | m4 e2e; inbox identity/claim/lease-expiry/archive-gap/rollback/concurrency/tier suite; durable MCP capture rollback/provenance tests; m10 guardrails; m12 phantom-org; capture/archive/session filesystem-mode tests | capture becomes manual filing | chokidar, PostgreSQL fenced claims | DECISIONS.md § 2026-06-16 — Classifier: completed-result captures mis-filed as open decisions (TODO, code fix) (+ same-day follow-up on the plain-task branch), § 2026-06-17 — Split compound "do X AND decide on Y" task captures (umbrella double-report fix), § 2026-07-01 — Minime owns person-vs-org classification (phantom-org root fix), § 2026-08-06 — Local runtime and engineering surfaces are closed by default, § 2026-08-06 — Inbox captures have immutable byte identity and fenced finalization |
| Importers | `src/importers/` (incl. pure RRULE/RDATE/EXDATE expansion in `src/importers/rrule.ts`), `src/util/clock.ts`, `db/migrations/031_calendar_occurrences.sql` | m4 golden-file suites (idempotency, UTC/`TZID`/floating/all-day calendar forms, invalid-date rejection); m9 overlap/gap DST clock cases; calendar-rrule suite (43 cases: pure FREQ/INTERVAL/COUNT/UNTIL/BYDAY expansion incl. a DST-transition, MONTHLY/YEARLY skip-not-clamp, and indefinite MONTHLY/YEARLY old-DTSTART fast-forward cases, EXDATE/RDATE, unsupported-RRULE safe degrade + audit event, re-import idempotency, future-only pruning on a changed rule incl. a shared-UID master + RECURRENCE-ID-override union fix, and the 031 backfill) | no calendar/tx/health/email mirrors | Intl timezone data | DECISIONS.md § 2026-08-06 — Calendar days and metric buckets use explicit time zones, § 2026-08-08 — Calendar occurrence identity: (uid, occurrence_start) expansion + future-only pruning |
| Dream job | `src/pipeline/dream.ts`, `src/serve.ts`, `src/util/metric-rollup.ts` (11 trusted maintenance steps scheduled in the configured owner timezone in the non-MCP supervisor; the owner executor is scoped only around the job); `src/db/repo.ts`'s `tryAcquireMaintenanceLock`/`releaseMaintenanceLock`/`lastEventAt`/`recentEventsByVerb`/`opsHealth`/`maintenanceLockHeld` (single-maintenance-owner advisory lock + dream missed-run catch-up, W3-5; content-free ops health facts + persistent-failure detection, W3-7); `src/ops/doctor.ts` (owner-run `minime doctor` CLI checklist over the same facts plus Postgres/Ollama/backup-dump/disk); `db/migrations/033_ops_failure_kind.sql`, `db/migrations/035_goal_review_kind.sql`; `src/db/client.ts`'s `adminSql` pool sizing; `src/ops/push.ts` (opt-in counts-only morning-brief notification, BRIEF_CRON, scheduled the same lock-winner-only way as dream/backup/restic-check, W3-11) | per-step suites (m9 notes, m12 watchdog, m14 validation); metric-time sum/streak-last/cache-identity/atomic-rebuild/mutable-source reconciliation/anomaly/due-date cases plus populated 025→026 rollback-safe upgrade; serve cron-timezone case; production-shaped contradiction pair suite; recurrence suite (dream sweeper crash-safety-net case); maintenance-lock suite (lock takeover, close()-releases-the-lock, backup-only-on-the-winner, catch-up-runs-once-when-stale, catch-up-not-at-all-when-fresh-or-brand-new; W3-9 extends every registration-order assertion to include the restic-check cron); doctor suite (opsHealth/recentEventsByVerb facts, 3-consecutive-failure ops_failure enqueue + dedupe-while-open, no-tier-leak stateSnapshot check, PASS/WARN/FAIL grading and exit code via injected statfs/Ollama probes, kind-constraint accept/reject, restic-check-age WARN line); push-brief suite (W3-11: adversarial no-title-leak counts-only rendering against real seeded rows, NTFY_URL loopback-only startup refusal, cron gated on BRIEF_CRON set + lock winner, delivery-failure routed to ops.log via run() without throwing, push:brief audit payload shape) | no nightly maintenance; backlog grows; without the lock, two resident `serve` processes duplicate dream/backup; without doctor/ops_health, a silently-broken maintenance pipeline has no owner-visible signal | croner | DECISIONS.md § 2026-06-10 — Dream-job simplifications (v1), § 2026-06-12 — Search uplift Phase 2: compiled-notes layer (dream step 2b_compile_notes), § 2026-06-12 — Near-real-time backup: 15-min tagged snapshots now, WAL PITR deferred (dbSnapshot extracted from dream.ts; step 7 unchanged), § 2026-07-01 — Minime owns person-vs-org classification (dream step 3b_phantom_persons watchdog), § 2026-07-27 — H5: production parent contradiction pairing, § 2026-08-06 — Calendar days and metric buckets use explicit time zones, § 2026-08-08 — Task recurrence primitive + habit_streak metric (dream step 5b_recurrence backfill), § 2026-08-07 — Single maintenance owner: advisory lock in serve + dream missed-run catch-up (also raises `adminSql` from max:1 to max:5, fixing a pre-existing deadlock in admin-scoped `compileNotes`), § 2026-08-09 — minime doctor CLI + ops_health + ops_failure review kind; net surface +1 (W3-7: `minime doctor` CLI — content-free PASS/WARN/FAIL maintenance checklist, no new MCP tool), § 2026-08-09 — W3-8: sanitized local ops log amends the H3 content-free-diagnostics contract (dream.ts's per-step catch, factored into exported `runDreamStep`, and serve.ts's cron-level catch each log a fixed step/cron label plus `error.constructor.name` — never the message — to data/logs/ops.log; see the Backup/recovery row for src/ops/ops-log.ts itself), § 2026-08-09 — W3-11: local push channel for a counts-only morning-brief notification (BRIEF_CRON, opt-in; net surface +0 MCP tools — a new local/loopback delivery surface, not an agent-facing interface), § 2026-08-09 — W3-12: goal_review dream kind (dream step 6b_goal_reviews — a 90d-stale, untouched-goal-and-untouched-linked-tasks flag, deduped like every other kind) and a goal search backfill (dream step 2d_goal_backlog_index, idempotent indexParent catch-up for any goal with no chunks yet); net surface +0 dream steps observable outside the MCP door row's own W3-12 clause (minime_state/minime_review_queue already cover the agent-facing surface) |
| Backup/recovery | `src/pipeline/backup.ts`, `src/ops/ops-log.ts`, `src/ops/doctor.ts`, `src/db/restore-schema-gate.ts`, `src/ops/snapshot-manifest.ts`, `src/ops/recovery-endpoints.ts`, `src/ops/restore-schema-gate.ts`, `src/util/config.ts`, `src/util/postgres-url.ts`, `src/util/libpq-service.ts`, `scripts/setup-env.sh`, `scripts/recovery-ops.ts`, `scripts/libpq-service.ts`, `scripts/snapshot-manifest.ts`, `scripts/validate-recovery-endpoints.ts`, `scripts/restore-*.sh`, `scripts/promote-restore.sh`, `scripts/pick-snapshot.ts`, `scripts/verify-restic-roundtrip.ts` (W3-9 adds a fail-closed disk-headroom preflight before pg_dump and a weekly `resticCheck()` — `restic check --read-data-subset=5%`, scheduled via `RESTIC_CHECK_CRON`/`config.resticCheckCron`, default `7 4 * * 0` -- deliberately off BACKUP_CRON's default `*/15 * * * *` grid so the two shipped defaults never collide on resticCheck()/runBackup()'s shared in-flight flag (review fix) -- same restic-configured + lock-winner guard as the db-snapshot cron) | backup/setup/manifest suites; W3-9 backup-preflight suite (disk headroom fails closed below the flat 256MB floor and below the scaled 2x-existing-dump threshold with pg_dump never spawned either way; `backup:restic-check` success/failure audit payloads are exactly `{ok}`; restic-check cron registers only when restic is configured and only for the maintenance-lock winner); ops-log suite (fixed allowlist classification, 0700/0600 file placement, single-generation rotation, adversarial no-leak case, dream-step/backup-command integration); hostile env/split-endpoint and exact schema-posture tests; forced-rename compensation; isolated fictional-data PostgreSQL + real restic round trip | one bad write is unrecoverable | restic, pg_dump, repository-stable dump roots, private libpq service files | DECISIONS.md § 2026-06-11 — Backups may target cloud object storage (encrypted restic repos), § 2026-06-12 — Near-real-time backup: 15-min tagged snapshots now, WAL PITR deferred, § 2026-06-15 — Setup wizard shows the generated restic password once, § 2026-07-23 — H3: repository-stable archive and dump roots, § 2026-08-06 — Recovery evidence is source-labelled and promotion is compensating, § 2026-08-09 — W3-8: sanitized local ops log amends the H3 content-free-diagnostics contract; net surface +1 (src/ops/ops-log.ts: allowlist-only stderr classification + owner-only mode-0600 local log, no new MCP tool, no audit-shape change); W3-9 (disk-headroom preflight + weekly restic check + one `minime doctor` line) has no dedicated entry — routine reliability plumbing per this file's own routine-fix carve-out, not a new invariant, privacy/egress, schema, interface, dependency, or recovery-semantics change; gap noted, not backfilled here |
| MinimeBench harness | `src/search/eval.ts`, `scripts/eval-search.ts`, `scripts/eval-search-worker.ts`, `fixtures/qrels/` | read-only mock regression gate (sealed gold, committed floors); explicit live/release scorecards; exact owned evaluator-temp cleanup on success/failure | flying blind on retrieval changes | guarded scratch DB wrapper | DECISIONS.md § 2026-06-12 — MinimeBench: area-based retrieval eval harness (Phase 1b), § 2026-06-12 — MinimeBench incident + corrections: scratch-DB guard; scorecard destination, § 2026-06-12 — Stability discipline: retrieval-regression gate in verify + CI, release snapshots, § 2026-06-12 — Regression-gate tolerance widened to 0.03 (cross-env HNSW jitter) |
| Public benchmarks | `scripts/eval-longmemeval.ts`, `eval-precisionmembench.ts`, `pmb-server.ts` | external reference points (LongMemEval-s recall@5 97.2%, docs/benchmarks/2026-06-12-longmemeval-s.md; PMB precision 52.3%, docs/benchmarks/2026-06-12-live-rerank-precisionmembench.md) | lose outside calibration | HF datasets, PMB_DIR clone | DECISIONS.md § 2026-06-12 — LongMemEval-s public benchmark runner (500 questions, judge-free), § 2026-06-12 — PrecisionMemBench runner (89 cases, judge-free retrieval precision) |
| SkillEval + SkillOpt | `scripts/eval-skills.ts`, `skill-eval-lib.ts`, `optimize-skill.ts` | SkillEval behavioral contracts; optimizer validated on suite `query` | skills evolve unmeasured | live model creds | DECISIONS.md § 2026-06-12 — SkillEval: behavioral eval for the skills layer (agents/skills/*.md), § 2026-06-12 — SkillOpt: validation-gated optimizer loop for the skills layer (+ same-day "target/optimizer role split" addendum) |
| Access-frequency boost | migrations `011_access_index.sql`, `020_audit_disposition.sql`; boost in `hybrid.ts` (±0.05 band) | **eval-calibration pending** — H4 access regression counts only `delivery:"transport"` exact `tool:minime_get_context` results correlated to a `released` disposition; historical, direct, suppressed, uncertain, and incomplete rows are ineligible; live calibration remains pending | delete = remove one multiplier; zero floor impact today | events table | DECISIONS.md § 2026-06-12 — agentmemory learnings: SessionEnd episodic capture + access-frequency rank nudge, § 2026-06-12 — Amendment: invariant-review hardening of the agentmemory learnings (pins the partial index to 011_access_index.sql), § 2026-07-23 — H4: raw-receipt attempt and pre-release result auditing, § 2026-07-26 — H4 result authorization, local disposition, and identity-owned facade |
| Session capture hook | `scripts/install-session-hook.sh`, `agents/hooks/` | session-capture suite | no episodic memory of coding sessions | Claude Code hooks | DECISIONS.md § 2026-06-12 — agentmemory learnings: SessionEnd episodic capture + access-frequency rank nudge, § 2026-06-12 — Amendment: invariant-review hardening of the agentmemory learnings (reclassifies session captures to tier 2) |

Plumbing (not subsystems): CLI/onboard/config/clock/install/verify plumbing: `src/cli.ts`,
`src/onboard.ts`, `src/db/`, `src/util/`, `src/verify/`, `.bun-version`, `Makefile`, and
`scripts/lib.sh`, `scripts/install.sh`, `scripts/up.sh`, `scripts/down.sh`,
`scripts/update.sh`, `scripts/verify-offline.sh` — glue, no independent eval, deleted only
with their owners.

## First-pass complexity-budget verdicts (2026-07)

- **Access-frequency boost** — still `eval-calibration pending` with a ±0.05 band. Verdict:
  justifying eval **scheduled** — piggyback a calibration arm on W8's live battery (same runs,
  boost on/off). If it cannot show a stable lift there, park it behind a flag and zero the band.
- **SkillOpt (optimizer loop)** — validated but bottlenecked on train-set coverage. Verdict:
  **parked** (owner-triggered `make optimize-skill` only); revisit after the improvement
  program; next step remains the gbrain-style self-rewriting loop over SkillEval task splits.
